extend type Query {
  catalogArtistNames: CatalogType!
  catalogCardNames: CatalogType!
  catalogLandTypes: CatalogType!
}

type CatalogType {
  object: String!
  uri: String!
  total_values: String!
  data: [String!]!
}

